# Caminho para um Chromium ja instalado na maquina.
# Necessario apenas em ambientes onde o Remotion nao pode baixar o proprio
# Chrome Headless Shell (rede restrita, CI com egress limitado).
# REMOTION_BROWSER_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
